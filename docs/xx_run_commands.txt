


Run backend: 

cd backend && source venv/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000




Run frontend: 

npm run dev --prefix frontend 



Start bedrock:

aws sso login --profile nice-dev

^^Dette går ikke på denne appen fordi du ikke har en cofig / kanskje CLI tool satt opp som kobler deg opp. hvordan var dette igjen?
du må ha den configen i .aws folderen på root et eller annet sted


